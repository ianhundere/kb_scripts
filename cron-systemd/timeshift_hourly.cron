# System crontab for Timeshift hourly checks
# Install to: /etc/cron.d/timeshift_hourly
#
# Runs Timeshift check every hour at 30 minutes past the hour
# Only runs if Borg backup is not currently running

SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin
MAILTO=""

30 * * * * root pgrep -lf python | grep borg >/dev/null; [[ $? -ne 0 ]] && timeshift --check --scripted
